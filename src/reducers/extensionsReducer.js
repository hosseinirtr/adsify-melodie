const initialState = {
  musicExtensions: ['.mp3', '.flac', '.m4a'],
  videoExtensions: ['.mp4', '.mkv', '.avi'],
};


const extensionsReducer = (state = initialState) => {
  return state
};

export default extensionsReducer;